import { USER_LOGIN_SUCESS } from '../constants/userConstants'

export const login = (email, password) => async (dispatch) => {
    try{
        type: USER_LOGIN_SUCESS

        //벡엔드에서 데이터 가져올 예정

        // 데이터를 payload에서 reducer로 넘김
    }catch (error) {
        
    }
}